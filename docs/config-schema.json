{
	"$defs": {
		"AthenaConfig": {
			"description": "Athena-specific configuration.",
			"properties": {
				"type": {
					"const": "athena",
					"default": "athena",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"s3_staging_dir": {
					"description": "S3 staging directory for query results",
					"title": "S3 Staging Dir",
					"type": "string"
				},
				"region_name": {
					"description": "AWS region name",
					"title": "Region Name",
					"type": "string"
				},
				"aws_access_key_id": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "AWS access key ID",
					"title": "Aws Access Key Id"
				},
				"aws_secret_access_key": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "AWS secret access key",
					"title": "Aws Secret Access Key"
				},
				"aws_session_token": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "AWS session token",
					"title": "Aws Session Token"
				},
				"profile_name": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "AWS profile name",
					"title": "Profile Name"
				},
				"schema_name": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Athena schema name",
					"title": "Schema Name"
				},
				"work_group": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": "primary",
					"description": "Athena workgroup",
					"title": "Work Group"
				}
			},
			"required": ["name", "s3_staging_dir", "region_name"],
			"title": "AthenaConfig",
			"type": "object"
		},
		"BigQueryConfig": {
			"description": "BigQuery-specific configuration.",
			"properties": {
				"type": {
					"const": "bigquery",
					"default": "bigquery",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"project_id": {
					"description": "GCP project ID",
					"title": "Project Id",
					"type": "string"
				},
				"dataset_id": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Default BigQuery dataset",
					"title": "Dataset Id"
				},
				"credentials_path": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Path to service account JSON file. If not provided, uses Application Default Credentials (ADC)",
					"title": "Credentials Path"
				},
				"credentials_json": {
					"anyOf": [
						{
							"additionalProperties": true,
							"type": "object"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Service account credentials as a dict or JSON string. Takes precedence over credentials_path if both are provided",
					"title": "Credentials Json"
				},
				"sso": {
					"default": false,
					"description": "Use Single Sign-On (SSO) for authentication",
					"title": "Sso",
					"type": "boolean"
				},
				"location": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "BigQuery location",
					"title": "Location"
				},
				"partition_filters": {
					"additionalProperties": {
						"type": "string"
					},
					"default": {},
					"description": "Custom partition filter expressions per table name, used when previewing tables that require a partition filter. Overrides the automatic last-partition detection. Example: {\"events\": \"event_date >= DATE_SUB(CURRENT_DATE(), INTERVAL 7 DAY)\"}",
					"title": "Partition Filters",
					"type": "object"
				},
				"max_query_size": {
					"anyOf": [
						{
							"type": "number"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Maximum query size in GB. If set, a dry run is performed before executing SQL and an error is raised if the estimated bytes processed exceeds this limit.",
					"title": "Max Query Size"
				}
			},
			"required": ["name", "project_id"],
			"title": "BigQueryConfig",
			"type": "object"
		},
		"ClickHouseConfig": {
			"description": "ClickHouse-specific configuration.",
			"properties": {
				"type": {
					"const": "clickhouse",
					"default": "clickhouse",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"host": {
					"description": "ClickHouse server host",
					"title": "Host",
					"type": "string"
				},
				"port": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "HTTP port (8123 plain, 8443 secure)",
					"title": "Port"
				},
				"database": {
					"description": "Database name",
					"title": "Database",
					"type": "string"
				},
				"user": {
					"description": "Username",
					"title": "User",
					"type": "string"
				},
				"password": {
					"default": "",
					"description": "Password",
					"title": "Password",
					"type": "string"
				},
				"secure": {
					"default": false,
					"description": "Use HTTPS",
					"title": "Secure",
					"type": "boolean"
				},
				"connect_timeout": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Connection timeout in seconds (passed to ibis.clickhouse.connect).",
					"title": "Connect Timeout"
				},
				"send_receive_timeout": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Send/receive timeout in seconds (passed to ibis.clickhouse.connect).",
					"title": "Send Receive Timeout"
				},
				"accessors": {
					"description": "Which default templates to render per table. Defaults to all.",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Accessors",
					"type": "array"
				}
			},
			"required": ["name", "host", "database", "user"],
			"title": "ClickHouseConfig",
			"type": "object"
		},
		"DatabaseTemplate": {
			"description": "Available default templates for database sync.",
			"enum": ["columns", "preview", "profiling", "ai_summary", "how_to_use"],
			"title": "DatabaseTemplate",
			"type": "string"
		},
		"DatabricksConfig": {
			"description": "Databricks-specific configuration.",
			"properties": {
				"type": {
					"const": "databricks",
					"default": "databricks",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"server_hostname": {
					"description": "Databricks server hostname (e.g., 'adb-xxxx.azuredatabricks.net')",
					"title": "Server Hostname",
					"type": "string"
				},
				"http_path": {
					"description": "HTTP path to the SQL warehouse or cluster",
					"title": "Http Path",
					"type": "string"
				},
				"access_token": {
					"description": "Databricks personal access token",
					"title": "Access Token",
					"type": "string"
				},
				"catalog": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Unity Catalog name (optional)",
					"title": "Catalog"
				},
				"schema_name": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Default schema (optional)",
					"title": "Schema Name"
				}
			},
			"required": ["name", "server_hostname", "http_path", "access_token"],
			"title": "DatabricksConfig",
			"type": "object"
		},
		"DuckDBConfig": {
			"description": "DuckDB-specific configuration.",
			"properties": {
				"type": {
					"const": "duckdb",
					"default": "duckdb",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"path": {
					"default": ":memory:",
					"description": "Path to the DuckDB database file",
					"title": "Path",
					"type": "string"
				}
			},
			"required": ["name"],
			"title": "DuckDBConfig",
			"type": "object"
		},
		"FabricAuthMode": {
			"enum": ["sql_password", "azure_interactive", "azure_cli", "azure_service_principal"],
			"title": "FabricAuthMode",
			"type": "string"
		},
		"FabricConfig": {
			"description": "Microsoft Fabric Data Warehouse / SQL Analytics Endpoint configuration.",
			"properties": {
				"type": {
					"const": "fabric",
					"default": "fabric",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"host": {
					"description": "Fabric SQL endpoint (e.g., 'abc123.datawarehouse.fabric.microsoft.com')",
					"title": "Host",
					"type": "string"
				},
				"port": {
					"default": 1433,
					"description": "SQL port",
					"title": "Port",
					"type": "integer"
				},
				"database": {
					"description": "Warehouse or Lakehouse name",
					"title": "Database",
					"type": "string"
				},
				"auth_mode": {
					"$ref": "#/$defs/FabricAuthMode",
					"default": "sql_password",
					"description": "Authentication method: 'sql_password', 'azure_interactive', 'azure_cli', or 'azure_service_principal'"
				},
				"user": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "SQL username",
					"title": "User"
				},
				"password": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "SQL password",
					"title": "Password"
				},
				"client_id": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Azure AD application (client) ID",
					"title": "Client Id"
				},
				"client_secret": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Azure AD client secret",
					"title": "Client Secret"
				},
				"tenant_id": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Azure AD tenant ID (optional)",
					"title": "Tenant Id"
				},
				"schema_name": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Default schema (optional)",
					"title": "Schema Name"
				},
				"driver": {
					"description": "ODBC driver \u2014 must be 'ODBC Driver 17/18 for SQL Server' (not FreeTDS)",
					"title": "Driver",
					"type": "string"
				}
			},
			"required": ["name", "host", "database"],
			"title": "FabricConfig",
			"type": "object"
		},
		"LLMConfig": {
			"description": "LLM configuration.",
			"properties": {
				"provider": {
					"$ref": "#/$defs/LLMProvider",
					"description": "The LLM provider to use"
				},
				"api_key": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "The API key to use",
					"title": "Api Key"
				},
				"base_url": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Optional custom base URL for the provider API",
					"title": "Base Url"
				},
				"access_key": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "AWS access key (only for Bedrock)",
					"title": "Access Key"
				},
				"secret_key": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "AWS secret key (only for Bedrock)",
					"title": "Secret Key"
				},
				"aws_region": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "AWS region (only for Bedrock)",
					"title": "Aws Region"
				},
				"aws_profile": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "AWS CLI profile name (only for Bedrock, e.g. SSO profile)",
					"title": "Aws Profile"
				},
				"gcp_project": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "GCP project ID (only for Vertex)",
					"title": "Gcp Project"
				},
				"gcp_location": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "GCP location (only for Vertex)",
					"title": "Gcp Location"
				},
				"service_account_json": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Service account JSON (only for Vertex)",
					"title": "Service Account Json"
				},
				"key_file": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Path to service account key file (only for Vertex)",
					"title": "Key File"
				},
				"annotation_model": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Model to use for ai_summary generation via prompt(...) in Jinja templates",
					"title": "Annotation Model"
				}
			},
			"required": ["provider"],
			"title": "LLMConfig",
			"type": "object"
		},
		"LLMProvider": {
			"description": "Supported LLM providers.",
			"enum": ["openai", "anthropic", "mistral", "gemini", "openrouter", "ollama", "bedrock", "vertex"],
			"title": "LLMProvider",
			"type": "string"
		},
		"McpConfig": {
			"description": "MCP (Model Context Protocol) configuration.",
			"properties": {
				"json_file_path": {
					"description": "Path to the MCP JSON configuration file",
					"title": "Json File Path",
					"type": "string"
				}
			},
			"required": ["json_file_path"],
			"title": "McpConfig",
			"type": "object"
		},
		"MssqlConfig": {
			"description": "Microsoft SQL Server configuration.",
			"properties": {
				"type": {
					"const": "mssql",
					"default": "mssql",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"host": {
					"description": "MSSQL host",
					"title": "Host",
					"type": "string"
				},
				"port": {
					"default": 1433,
					"description": "MSSQL port",
					"title": "Port",
					"type": "integer"
				},
				"database": {
					"description": "Database name",
					"title": "Database",
					"type": "string"
				},
				"user": {
					"description": "Username",
					"title": "User",
					"type": "string"
				},
				"password": {
					"description": "Password",
					"title": "Password",
					"type": "string"
				},
				"driver": {
					"description": "ODBC driver (FreeTDS on Mac/Linux, ODBC Driver 18 for SQL Server on Windows)",
					"title": "Driver",
					"type": "string"
				},
				"schema_name": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Default schema (optional, uses 'dbo' if not set)",
					"title": "Schema Name"
				}
			},
			"required": ["name", "host", "database", "user", "password"],
			"title": "MssqlConfig",
			"type": "object"
		},
		"MysqlConfig": {
			"description": "MySQL-specific configuration.",
			"properties": {
				"type": {
					"const": "mysql",
					"default": "mysql",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"host": {
					"description": "MySQL host",
					"title": "Host",
					"type": "string"
				},
				"port": {
					"default": 3306,
					"description": "MySQL port",
					"title": "Port",
					"type": "integer"
				},
				"database": {
					"description": "Database name",
					"title": "Database",
					"type": "string"
				},
				"user": {
					"description": "Username",
					"title": "User",
					"type": "string"
				},
				"password": {
					"description": "Password",
					"title": "Password",
					"type": "string"
				},
				"schema_name": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Default schema (optional)",
					"title": "Schema Name"
				}
			},
			"required": ["name", "host", "database", "user", "password"],
			"title": "MysqlConfig",
			"type": "object"
		},
		"NotionConfig": {
			"description": "Notion configuration.",
			"properties": {
				"api_key": {
					"description": "The API key to use",
					"title": "Api Key",
					"type": "string"
				},
				"pages": {
					"description": "The pages to sync",
					"items": {
						"type": "string"
					},
					"title": "Pages",
					"type": "array"
				}
			},
			"required": ["api_key", "pages"],
			"title": "NotionConfig",
			"type": "object"
		},
		"PostgresConfig": {
			"description": "PostgreSQL-specific configuration.",
			"properties": {
				"type": {
					"const": "postgres",
					"default": "postgres",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"host": {
					"description": "PostgreSQL host",
					"title": "Host",
					"type": "string"
				},
				"port": {
					"default": 5432,
					"description": "PostgreSQL port",
					"title": "Port",
					"type": "integer"
				},
				"database": {
					"description": "Database name",
					"title": "Database",
					"type": "string"
				},
				"user": {
					"description": "Username",
					"title": "User",
					"type": "string"
				},
				"password": {
					"description": "Password",
					"title": "Password",
					"type": "string"
				},
				"schema_name": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Default schema (optional, uses 'public' if not set)",
					"title": "Schema Name"
				}
			},
			"required": ["name", "host", "database", "user", "password"],
			"title": "PostgresConfig",
			"type": "object"
		},
		"ProfilingConfig": {
			"description": "Configuration for profiling refresh policy.",
			"properties": {
				"refresh_policy": {
					"$ref": "#/$defs/ProfilingRefreshPolicy",
					"default": "always",
					"description": "When to recompute profiling: always, interval, or once"
				},
				"interval_days": {
					"default": 7,
					"description": "Number of days between profiling runs (only used when refresh_policy=interval)",
					"minimum": 1,
					"title": "Interval Days",
					"type": "integer"
				}
			},
			"title": "ProfilingConfig",
			"type": "object"
		},
		"ProfilingRefreshPolicy": {
			"enum": ["always", "interval", "once"],
			"title": "ProfilingRefreshPolicy",
			"type": "string"
		},
		"RedshiftAuthMode": {
			"enum": ["password", "azure_entra_id"],
			"title": "RedshiftAuthMode",
			"type": "string"
		},
		"RedshiftConfig": {
			"description": "Amazon Redshift-specific configuration.",
			"properties": {
				"type": {
					"const": "redshift",
					"default": "redshift",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"host": {
					"description": "Redshift cluster endpoint",
					"title": "Host",
					"type": "string"
				},
				"port": {
					"default": 5439,
					"description": "Redshift port",
					"title": "Port",
					"type": "integer"
				},
				"database": {
					"description": "Database name",
					"title": "Database",
					"type": "string"
				},
				"auth_mode": {
					"$ref": "#/$defs/RedshiftAuthMode",
					"default": "password",
					"description": "Authentication mode"
				},
				"user": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Username. Required for password auth. In azure_entra_id mode it is optional but recommended: nao sync uses it to read metadata (columns, previews, query history). It is never used at runtime from /execute_sql.",
					"title": "User"
				},
				"password": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Password. Required for password auth. In azure_entra_id mode it is optional but recommended: nao sync uses it to read metadata (columns, previews, query history). It is never used at runtime from /execute_sql.",
					"title": "Password"
				},
				"schema_name": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Default schema (optional, uses 'public' if not set)",
					"title": "Schema Name"
				},
				"sslmode": {
					"default": "require",
					"description": "SSL mode for the connection",
					"title": "Sslmode",
					"type": "string"
				},
				"ssh_tunnel": {
					"anyOf": [
						{
							"$ref": "#/$defs/RedshiftSSHTunnelConfig"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "SSH tunnel configuration (optional)"
				}
			},
			"required": ["name", "host", "database"],
			"title": "RedshiftConfig",
			"type": "object"
		},
		"RedshiftSSHTunnelConfig": {
			"description": "SSH tunnel configuration for Redshift connection.",
			"properties": {
				"ssh_host": {
					"description": "SSH host",
					"title": "Ssh Host",
					"type": "string"
				},
				"ssh_port": {
					"default": 22,
					"description": "SSH port",
					"title": "Ssh Port",
					"type": "integer"
				},
				"ssh_username": {
					"description": "SSH username",
					"title": "Ssh Username",
					"type": "string"
				},
				"ssh_private_key_path": {
					"description": "Path to SSH private key file",
					"title": "Ssh Private Key Path",
					"type": "string"
				},
				"ssh_private_key_passphrase": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "SSH private key passphrase (optional)",
					"title": "Ssh Private Key Passphrase"
				}
			},
			"required": ["ssh_host", "ssh_username", "ssh_private_key_path"],
			"title": "RedshiftSSHTunnelConfig",
			"type": "object"
		},
		"RepoConfig": {
			"description": "Repository configuration.\n\nSupports two source types:\n- Git repository: specify `url` (and optionally `branch`)\n- Local path: specify `local_path` (relative to nao_config.yaml or absolute)",
			"properties": {
				"name": {
					"description": "The name of the repository",
					"title": "Name",
					"type": "string"
				},
				"url": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "The URL of the repository",
					"title": "Url"
				},
				"branch": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "The branch of the repository",
					"title": "Branch"
				},
				"local_path": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Local filesystem path (relative to nao_config.yaml or absolute)",
					"title": "Local Path"
				},
				"include": {
					"description": "Glob patterns for files to include (e.g. 'models/**/*.sql'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for files to exclude (e.g. '*.pyc')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				}
			},
			"required": ["name"],
			"title": "RepoConfig",
			"type": "object"
		},
		"SkillsConfig": {
			"description": "Skills configuration.",
			"properties": {
				"folder_path": {
					"description": "Path to the skills folder",
					"title": "Folder Path",
					"type": "string"
				}
			},
			"required": ["folder_path"],
			"title": "SkillsConfig",
			"type": "object"
		},
		"SlackConfig": {
			"description": "Slack configuration.",
			"properties": {
				"bot_token": {
					"description": "The bot token to use",
					"title": "Bot Token",
					"type": "string"
				},
				"signing_secret": {
					"description": "The signing secret for verifying requests",
					"title": "Signing Secret",
					"type": "string"
				},
				"post_message_url": {
					"default": "https://slack.com/api/chat.postMessage",
					"description": "The Slack API URL for posting messages",
					"title": "Post Message Url",
					"type": "string"
				}
			},
			"required": ["bot_token", "signing_secret"],
			"title": "SlackConfig",
			"type": "object"
		},
		"SnowflakeConfig": {
			"description": "Snowflake-specific configuration.",
			"properties": {
				"type": {
					"const": "snowflake",
					"default": "snowflake",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"username": {
					"description": "Snowflake username",
					"title": "Username",
					"type": "string"
				},
				"account_id": {
					"description": "Snowflake account identifier (e.g., 'xy12345.us-east-1')",
					"title": "Account Id",
					"type": "string"
				},
				"password": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Snowflake password",
					"title": "Password"
				},
				"database": {
					"description": "Snowflake database",
					"title": "Database",
					"type": "string"
				},
				"schema_name": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Snowflake schema (optional)",
					"title": "Schema Name"
				},
				"warehouse": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Snowflake warehouse to use (optional)",
					"title": "Warehouse"
				},
				"private_key_path": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Path to private key file for key-pair authentication",
					"title": "Private Key Path"
				},
				"private_key": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "PEM-encoded private key string for key-pair authentication (alternative to private_key_path)",
					"title": "Private Key"
				},
				"passphrase": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Passphrase for the private key if it is encrypted",
					"title": "Passphrase"
				},
				"authenticator": {
					"anyOf": [
						{
							"enum": ["externalbrowser", "username_password_mfa", "jwt_token", "oauth"],
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Authentication method (e.g., 'externalbrowser' for SSO)",
					"title": "Authenticator"
				}
			},
			"required": ["name", "username", "account_id", "database"],
			"title": "SnowflakeConfig",
			"type": "object"
		},
		"StarRocksConfig": {
			"description": "StarRocks-specific configuration using mysql-connector-python.",
			"properties": {
				"type": {
					"const": "starrocks",
					"default": "starrocks",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"host": {
					"description": "StarRocks FE host",
					"title": "Host",
					"type": "string"
				},
				"port": {
					"default": 9030,
					"description": "StarRocks MySQL protocol port",
					"title": "Port",
					"type": "integer"
				},
				"user": {
					"description": "Username",
					"title": "User",
					"type": "string"
				},
				"password": {
					"default": "",
					"description": "Password",
					"title": "Password",
					"type": "string"
				},
				"catalog": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Catalog name (optional, defaults to all catalogs)",
					"title": "Catalog"
				},
				"database": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Default database name (optional)",
					"title": "Database"
				},
				"schema_name": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Specific schema to sync (optional)",
					"title": "Schema Name"
				}
			},
			"required": ["name", "host", "user"],
			"title": "StarRocksConfig",
			"type": "object"
		},
		"TrinoConfig": {
			"description": "Trino-specific configuration.",
			"properties": {
				"type": {
					"const": "trino",
					"default": "trino",
					"title": "Type",
					"type": "string"
				},
				"name": {
					"description": "A friendly name for this connection",
					"title": "Name",
					"type": "string"
				},
				"include": {
					"description": "Glob patterns for schemas/tables to include (e.g., 'prod_*.*', 'analytics.dim_*'). Empty means include all.",
					"items": {
						"type": "string"
					},
					"title": "Include",
					"type": "array"
				},
				"exclude": {
					"description": "Glob patterns for schemas/tables to exclude (e.g., 'temp_*.*', '*.backup_*')",
					"items": {
						"type": "string"
					},
					"title": "Exclude",
					"type": "array"
				},
				"templates": {
					"description": "Which default templates to render per table (e.g., ['columns', 'how_to_use', 'profiling', 'ai_summary']). Defaults to ['columns', 'how_to_use', 'preview'].",
					"items": {
						"$ref": "#/$defs/DatabaseTemplate"
					},
					"title": "Templates",
					"type": "array"
				},
				"query_history_days": {
					"anyOf": [
						{
							"type": "integer"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Number of days to look back for query history (used by how_to_use template).",
					"title": "Query History Days"
				},
				"profiling": {
					"$ref": "#/$defs/ProfilingConfig",
					"description": "Profiling refresh policy configuration"
				},
				"host": {
					"description": "Trino coordinator host",
					"title": "Host",
					"type": "string"
				},
				"port": {
					"default": 8080,
					"description": "Trino coordinator port",
					"title": "Port",
					"type": "integer"
				},
				"catalog": {
					"description": "Catalog name",
					"title": "Catalog",
					"type": "string"
				},
				"user": {
					"description": "Username",
					"title": "User",
					"type": "string"
				},
				"schema_name": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Default schema (optional)",
					"title": "Schema Name"
				},
				"password": {
					"anyOf": [
						{
							"type": "string"
						},
						{
							"type": "null"
						}
					],
					"default": null,
					"description": "Password (optional)",
					"title": "Password"
				}
			},
			"required": ["name", "host", "catalog", "user"],
			"title": "TrinoConfig",
			"type": "object"
		}
	},
	"description": "nao project configuration.",
	"properties": {
		"project_name": {
			"description": "The name of the nao project",
			"title": "Project Name",
			"type": "string"
		},
		"databases": {
			"description": "The databases to use",
			"items": {
				"discriminator": {
					"mapping": {
						"athena": "#/$defs/AthenaConfig",
						"bigquery": "#/$defs/BigQueryConfig",
						"clickhouse": "#/$defs/ClickHouseConfig",
						"databricks": "#/$defs/DatabricksConfig",
						"duckdb": "#/$defs/DuckDBConfig",
						"fabric": "#/$defs/FabricConfig",
						"mssql": "#/$defs/MssqlConfig",
						"mysql": "#/$defs/MysqlConfig",
						"postgres": "#/$defs/PostgresConfig",
						"redshift": "#/$defs/RedshiftConfig",
						"snowflake": "#/$defs/SnowflakeConfig",
						"starrocks": "#/$defs/StarRocksConfig",
						"trino": "#/$defs/TrinoConfig"
					},
					"propertyName": "type"
				},
				"oneOf": [
					{
						"$ref": "#/$defs/AthenaConfig"
					},
					{
						"$ref": "#/$defs/BigQueryConfig"
					},
					{
						"$ref": "#/$defs/ClickHouseConfig"
					},
					{
						"$ref": "#/$defs/DatabricksConfig"
					},
					{
						"$ref": "#/$defs/FabricConfig"
					},
					{
						"$ref": "#/$defs/SnowflakeConfig"
					},
					{
						"$ref": "#/$defs/DuckDBConfig"
					},
					{
						"$ref": "#/$defs/MysqlConfig"
					},
					{
						"$ref": "#/$defs/MssqlConfig"
					},
					{
						"$ref": "#/$defs/PostgresConfig"
					},
					{
						"$ref": "#/$defs/RedshiftConfig"
					},
					{
						"$ref": "#/$defs/StarRocksConfig"
					},
					{
						"$ref": "#/$defs/TrinoConfig"
					}
				]
			},
			"title": "Databases",
			"type": "array"
		},
		"repos": {
			"description": "The repositories to use",
			"items": {
				"$ref": "#/$defs/RepoConfig"
			},
			"title": "Repos",
			"type": "array"
		},
		"notion": {
			"anyOf": [
				{
					"$ref": "#/$defs/NotionConfig"
				},
				{
					"type": "null"
				}
			],
			"default": null,
			"description": "The Notion configurations"
		},
		"llm": {
			"anyOf": [
				{
					"$ref": "#/$defs/LLMConfig"
				},
				{
					"type": "null"
				}
			],
			"default": null,
			"description": "The LLM configuration"
		},
		"slack": {
			"anyOf": [
				{
					"$ref": "#/$defs/SlackConfig"
				},
				{
					"type": "null"
				}
			],
			"default": null,
			"description": "The Slack configuration"
		},
		"mcp": {
			"anyOf": [
				{
					"$ref": "#/$defs/McpConfig"
				},
				{
					"type": "null"
				}
			],
			"default": null,
			"description": "The MCP configuration"
		},
		"skills": {
			"anyOf": [
				{
					"$ref": "#/$defs/SkillsConfig"
				},
				{
					"type": "null"
				}
			],
			"default": null,
			"description": "The Skills configuration"
		}
	},
	"required": ["project_name"],
	"title": "NaoConfig",
	"type": "object"
}
